import { LoadingScreen } from "@/components/LoadingScreen";

export default function RootLoading() {
  return (
    <LoadingScreen 
      message="กำลังเชื่อมต่อคลังข้อมูล Pokky Optimize..." 
      subMessage="ESPORTS SYSTEM OPTIMIZER"
    />
  );
}
